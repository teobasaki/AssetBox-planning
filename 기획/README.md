# AssetBox · 기획 문서 (PM 패키지)

> 이 폴더는 8명이 한 달 동안 같은 그림을 보고 일하기 위한 **단일 진실 소스 (Single Source of Truth)** 입니다.
> 코드보다 이 폴더가 먼저 바뀌고, 그 다음 코드가 따라옵니다.

---

## 비유로 먼저

- `**기획/` = 건축 도면 묶음.** 도면이 흐릿하면 벽 두 개가 어긋난 채 만나서 머지 충돌이 터집니다.
- **각 도메인 = 건물의 한 층.** 같은 콘센트 규격(= API 컨트랙트)만 지키면 층마다 다른 인테리어를 해도 전기는 통합니다.
- **PM = 건축가.** 도면을 그리고, 충돌을 미리 잡고, 일정의 진척을 추적합니다.
- **각 파트장 = 시공 책임자.** 자기 층은 자기가 책임지고, 도면을 바꿀 일이 생기면 다른 층과 협의 후 도면부터 수정합니다.

---

## 폴더 구성


| 폴더                  | 무엇이 들어있나                                   | 누가 가장 자주 보나     |
| ------------------- | ------------------------------------------ | --------------- |
| `00_overview/`      | 프로젝트 한 줄 정의·목표·용어집·읽는 순서                   | 전원              |
| `01_planning/`      | 전체 마일스톤, 스프린트, 역할 분담표, 의식(데일리/위클리)         | 전원 (PM이 매주 갱신)  |
| `02_architecture/`  | 시스템 아키텍처, 모듈 경계, 디렉토리 구조, 빌드/런타임           | Infra, 전 도메인 리드 |
| `03_erd/`           | ERD, 테이블 스키마, 외래키, 인덱스, 스냅샷 설계 사유          | 전 도메인           |
| `04_api/`           | REST 명세, WebSocket 명세, 인증/응답 표준, 에러 코드     | 전 도메인 (계약서)     |
| `05_ia_userflow/`   | 사이트맵(IA), 역할별 유저플로우, 핵심 시나리오               | 전 도메인           |
| `06_features/`      | 기능 카탈로그, MoSCoW 우선순위, MVP/v1/v1.1 컷, 인수 기준 | 전 도메인           |
| `07_part_guides/`   | **파트별(8개) 구현 가이드라인** — 일감, 컨트랙트, 체크리스트     | 각 파트 담당자        |
| `08_collaboration/` | Git/PR/코딩 컨벤션, 머지 전략, 충돌 방지, Red Team Review, Slack 채널 설정 | 전원 (필독)         |


---

## 처음 들어온 사람의 30분 온보딩 코스

1. `00_overview/00_프로젝트_개요.md` — 무엇을 만드는지 (5분)
2. `01_planning/01_마일스톤_일정.md` — 한 달이 어떻게 흘러가는지 (5분)
3. `01_planning/02_역할_분담표.md` — 내 자리 + 옆자리 (3분)
4. `02_architecture/01_시스템_아키텍처.md` — 큰 그림 (5분)
5. `08_collaboration/01_Git_브랜치_PR_규칙.md` — 코드 올리기 전 필수 (5분)
6. `**07_part_guides/내_파트.md`** — 내가 뭘 해야 하는지 (7분)

---

## 핵심 원칙 5가지 (TL;DR)

1. **컨트랙트가 먼저, 구현은 나중.** API 명세(`04_api/`)와 ERD(`03_erd/`)를 먼저 합의한 다음에 구현합니다. 컨트랙트는 PR이 아닌 *기획 변경 회의*로만 바꿉니다.
2. **남의 도메인 엔티티는 직접 import 하지 않습니다.** 필요하면 ID(Long) 만 들고, 조회가 필요하면 그 도메인의 service를 통합니다. (모듈러 모놀리스 원칙)
3. **DB 마이그레이션 책임은 한 명.** 같은 컬럼을 두 명이 동시에 만들면 100% 깨집니다. (Infra가 게이트키퍼)
4. **모든 응답은 `ApiResponse<T>` 포맷.** 프론트가 일관되게 다룰 수 있도록.
5. **테스트는 사치가 아니라 충돌 방지 장치.** 각 도메인 핵심 시나리오 1~2개는 머지 전 반드시 작성.

---

## 문서를 바꿀 때

- 본인 파트 가이드(`07_part_guides/`) 는 자유롭게 수정 → PR에 한 줄 코멘트로 알림
- API 컨트랙트(`04_api/`) / ERD(`03_erd/`) / 일정(`01_planning/`) 변경은 **PM 승인 필요** — Slack에 [기획변경] 태그로 제안
- 회의록은 `01_planning/회의록/` 에 날짜별로 추가

---

## 도메인 ↔ 담당자 커버리지 매트릭스 (2026-05-22 회의 반영)

> 8명의 책임 합이 실제 코드의 모든 도메인을 빠짐없이 덮는지 점검한 결과.


| 코드의 도메인 패키지             | 담당                                              | 비고                                                     |
| ----------------------- | ----------------------------------------------- | ------------------------------------------------------ |
| `com.assetbox.user`     | **User** (2명)                                   | Form + OAuth(Google/Naver), 이메일 화이트리스트, realName/major |
| `com.assetbox.file`     | **File** (2명, 페어 / 내부 분담)                       | 통합 도메인. 저장/조회/메타/삭제 + purpose 4종                       |
| `com.assetbox.post`     | **Post** (1명)                                   | 게시글 CRUD + linkedRequestId 자동 완료                       |
| `com.assetbox.request`  | **Request** (1명)                                | 요청 흐름. 어드민 개입 없음                                       |
| `com.assetbox.comment`  | **Comment + Category + Tag/Search** (1명)        | Post/Request 댓글, 카테고리 트리, 태그/검색                        |
| `com.assetbox.category` | (위와 동일)                                         |                                                        |
| `com.assetbox.tag`      | (위와 동일)                                         |                                                        |
| `com.assetbox.message`  | **DM** (1명)                                     | REST + WebSocket + 시스템 발신자                             |
| `com.assetbox.feedback` | **Infra ( 2명 )**                                | 작은 운영 도메인. 익명 차단(로그인 필수)                               |
| `com.assetbox.common.`* | **Infra (주) + User (security 일부) + DM (WS 일부)** | 공동 자원                                                  |


**결론**: 8명 , 모든 도메인이 단일 책임자 또는 명시된 페어로 덮였고, 공동 자원(`common/`)은 충돌 방지 규칙으로 보호된다.

> **본 MVP 범위 외 (회의 결정)**: 어드민 강제 삭제, 유저 제재, 메시지 강제 열람, DownloadLog/통계, 카카오 OAuth. 모두 v1.1 또는 별도 검토.

