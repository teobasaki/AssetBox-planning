# AssetBox

AssetBox backend planning and Spring Boot skeleton.

## Backend

```bash
cd assetbox
./gradlew clean build
```

If the Gradle wrapper is not generated yet, run with a local Gradle installation:

```bash
cd assetbox
gradle clean build
```
