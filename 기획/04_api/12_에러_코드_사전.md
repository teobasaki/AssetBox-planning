# 12. 에러 코드 사전 (전 도메인 통합)

> ⚠️ **2026-05-22 회의 반영분과 충돌 시** [`02_엔드포인트_목록_상태코드분리_수정본.md`](./02_엔드포인트_목록_상태코드분리_수정본.md) **이 진실의 단일 원본.**
>
> 회의 결과 다음 변경이 적용됨:
> - **추가**: OAuth / 이메일 화이트리스트 / profileRequired / linkedRequestId 관련 코드
> - **제거 (MVP 범위 외)**: 어드민 강제 삭제, 분쟁 처리, 어드민 메시지 열람 관련 코드 (`POST_HARD_DELETE_FAILED` 등은 사용 안 함)
> - **변경**: Feedback `FEEDBACK_RATE_LIMITED` 는 익명 차단으로 사실상 불필요

> 비유: **사전.** 어디서나 `POST_NOT_FOUND` 라는 단어를 던지면 모든 8명이 같은 뜻으로 받는다. 사전이 흔들리면 코드와 프론트 사이 통역이 깨진다.

> 본 사전은 명세서(03~11)에 등장한 모든 `error.code` 값을 한 곳에 모은 단일 진실 소스입니다. 새 코드 추가 시 본 문서 갱신 후 사용.

---

## 0) 사용 규칙

1. **이 표에 없는 코드는 던지지 않는다.** 새 코드가 필요하면 본 문서에 먼저 추가 (PR 한 줄).
2. **메시지(한국어)는 가이드일 뿐.** 실제 응답 메시지는 사용 맥락에 맞게 더 상세히 써도 됨. 단 코드는 동일.
3. **검증 실패는 `VALIDATION_FAILED` 한 코드로 통일.** 필드별 상세는 `error.fields[]` 로 전달 (`01_API_표준.md` 1-3).
4. 한 엔드포인트가 같은 의미를 여러 코드로 던지지 않는다. (`POST_NOT_FOUND` 와 `POST_DOES_NOT_EXIST` 같은 중복 금지)

---

## 1) 공통 (Common)

| code | HTTP | 의미 / 발생 조건 |
|---|---|---|
| `VALIDATION_FAILED` | 400 | Bean Validation 실패 (`@NotBlank`, `@Size`, `@Email` 등). `error.fields[]` 포함 |
| `PAGINATION_SIZE_TOO_LARGE` | 400 | `size > 50` |
| `SORT_KEY_NOT_ALLOWED` | 400 | sort 키 화이트리스트 외 |
| `LIMIT_TOO_LARGE` | 400 | top-N 류 `limit` 가 최대값 초과 |
| `DATE_RANGE_INVALID` | 400 | `from > to` |
| `UNAUTHORIZED` | 401 | 토큰 누락 |
| `TOKEN_EXPIRED` | 401 | JWT exp 지남 |
| `TOKEN_INVALID` | 401 | 시그니처 위반 / 형식 오류 |
| `FORBIDDEN` | 403 | 인증은 됐으나 권한 부족 (일반) |
| `INTERNAL_ERROR` | 500 | 그 외 (스택트레이스 비노출) |

---

## 2) User · 인증

| code | HTTP | 의미 / 발생 조건 |
|---|---|---|
| `USER_NOT_FOUND` | 404 | 대상 user id 미존재 |
| `USER_EMAIL_DUPLICATED` | 409 | 이미 가입된 이메일 |
| `USER_NICKNAME_DUPLICATED` | 409 | (정책 적용 시) 닉네임 중복 |
| `USER_EMAIL_NOT_WHITELISTED` | 403 | 가입 시도 이메일이 화이트리스트 미포함 |
| `USER_PROFILE_INCOMPLETE` | 403 | `major == null` 인 상태에서 보호된 페이지 접근 시도. 프론트는 "내 정보 보완 페이지"로 리다이렉트 |
| `USER_REALNAME_NOT_EDITABLE` | 403 | 일반 USER가 realName/major 수정 시도 (SUPER_ADMIN만 가능) |
| `USER_AVATAR_NOT_FOUND` | 404 | 아바타 미설정 또는 파일 없음 |
| `LOGIN_FAILED` | 401 | 비밀번호 불일치 또는 이메일 미존재 (둘을 구분하지 않음 — 보안) |
| `OAUTH_PROVIDER_NOT_SUPPORTED` | 400 | google / naver 외의 provider 요청 |
| `OAUTH_EMAIL_NOT_WHITELISTED` | 403 | OAuth 로 받은 이메일이 화이트리스트 미포함 |
| `OAUTH_LINK_CONFLICT` | 409 | 같은 이메일이 다른 provider 로 이미 가입됨 |
| `FORBIDDEN_SELF_ROLE_CHANGE` | 403 | 본인 role 변경 시도 |

> ❌ **본 MVP 범위 외** (사용 금지): `USER_HAS_DEPENDENCIES`, `FORBIDDEN_SELF_DELETE` — 어드민 강제 삭제·제재 빠지면서 같이 제거.

---

## 3) Post

| code | HTTP | 의미 / 발생 조건 |
|---|---|---|
| `POST_NOT_FOUND` | 404 | postId 미존재 |
| `POST_DELETED` | 410 | soft delete된 글 (일반 사용자) |
| `POSTLIKE_DUPLICATED` | 409 | (현재 토글로 처리하므로 미사용. 예약) |
| `POST_LINKED_REQUEST_NOT_FOUND` | 404 | `linkedRequestId` 가 존재하지 않는 요청 |
| `POST_LINKED_REQUEST_FORBIDDEN` | 403 | `linkedRequestId` 가 본인이 assignee가 아닌 요청 |
| `POST_LINKED_REQUEST_INVALID_STATUS` | 400 | 연결 시도한 요청이 IN_PROGRESS 상태 아님 |
| `POST_LINKED_REQUEST_ALREADY_COMPLETED` | 409 | 이미 다른 게시글로 완료된 요청에 다시 연결 시도 |

> ❌ **본 MVP 범위 외**: `POST_HARD_DELETE_*` — 어드민 강제 삭제 제외.

---

## 4) Request (요청 게시판) — 어드민 개입 없음

| code | HTTP | 의미 / 발생 조건 |
|---|---|---|
| `REQUEST_NOT_FOUND` | 404 | |
| `REQUEST_DELETED` | 410 | soft delete된 요청 |
| `REQUEST_NOT_EDITABLE` | 400 | status 가 REQUESTED 아님 (수정 시) |
| `REQUEST_STATUS_UNKNOWN` | 400 | status 값이 enum 아님 |
| `REQUEST_STATUS_TRANSITION_INVALID` | 400 | 허용 외 상태 전이 |
| `REQUEST_ASSIGN_TAKEN` | 409 | `/assign` 호출했으나 이미 다른 assignee 가 있음 |
| `REQUEST_ASSIGN_SELF_DUPLICATED` | 409 | 본인이 이미 assignee 인데 다시 assign 시도 |
| `REQUEST_REJECT_FORBIDDEN` | 403 | reject 권한이 assignee/요청자 아님 |
| `REQUEST_REOPEN_FORBIDDEN` | 403 | reopen 권한이 요청자 아님 |
| `REQUEST_REOPEN_INVALID_STATUS` | 400 | 현 status가 REJECTED 아님 |
| `REQUEST_ALREADY_COMPLETED` | 409 | 이미 COMPLETED 상태에서 변경 / link 시도 |
| `REQUEST_COMPLETED_LOCKED` | 409 | COMPLETED/REJECTED 인데 변경 시도 |
| `REQUEST_IN_PROGRESS_LOCKED` | 409 | 진행 중인 요청 삭제 시도 |
| `REQUEST_DEADLINE_PAST` | 400 | deadline 과거 (정책 합의) |

> ❌ **본 MVP 범위 외** (사용 금지): `REQUEST_LINK_NOT_IN_PROGRESS` — 별도 `/link-post` 엔드포인트 제거. 게시글 작성 시 `linkedRequestId` 로 자동 처리되므로 `POST_LINKED_REQUEST_INVALID_STATUS` 가 그 역할 대체.

---

## 5) Comment

| code | HTTP | 의미 / 발생 조건 |
|---|---|---|
| `COMMENT_NOT_FOUND` | 404 | |
| `COMMENT_PARENT_NOT_FOUND` | 404 | parentId 미존재 |
| `COMMENT_PARENT_MISMATCH` | 400 | parent가 다른 글의 댓글 |
| `COMMENT_POST_MISMATCH` | 400 | commentId 가 다른 post 의 댓글 (Post 댓글 삭제 시) |
| `COMMENT_REQUEST_MISMATCH` | 400 | commentId 가 다른 request 의 댓글 |
| `COMMENT_NESTED_TOO_DEEP` | 400 | 1단계 이상 중첩 시도 |
| `COMMENT_PARENT_DELETED` | 400 | parent가 soft delete된 상태 |
| `COMMENT_ALREADY_DELETED` | 410 | 이미 soft delete |

---

## 6) Category / Tag

| code | HTTP | 의미 / 발생 조건 |
|---|---|---|
| `CATEGORY_NOT_FOUND` | 404 | |
| `CATEGORY_DEPTH_EXCEEDED` | 400 | depth>3 시도 |
| `CATEGORY_NAME_DUPLICATED_IN_PARENT` | 400 | 같은 부모 아래 동일 이름 |
| `CATEGORY_HAS_CHILDREN` | 409 | 자식 있어 삭제 거부 |
| `CATEGORY_HAS_POSTS` | 409 | 게시글 있어 삭제 거부 |
| `TAG_NAME_TOO_LONG` | 400 | 30자 초과 |
| `TAG_NAME_INVALID_CHAR` | 400 | 허용 외 특수문자 |

---

## 7) File / Storage (통합 도메인)

| code | HTTP | 의미 / 발생 조건 |
|---|---|---|
| `FILE_NOT_FOUND` | 404 | fileId 미존재 |
| `FILE_PURPOSE_MISMATCH` | 400 | purpose(ASSET/POST_THUMBNAIL/USER_AVATAR/REQUEST_REFERENCE) 가 호출 컨텍스트와 다름 |
| `FILE_OWNER_MISMATCH` | 403 | 소유자가 아닌 사용자의 삭제 시도 |
| `FILE_EMPTY` | 400 | 0바이트 파일 |
| `FILE_EXTENSION_NOT_ALLOWED` | 400 | 허용 외 확장자 (purpose별 화이트리스트 다름) |
| `FILE_TOO_LARGE` | 400 | 사이즈 한도 초과 (에셋 20MB, 썸네일/아바타/참고이미지 1MB) |
| `FILE_DELETED` | 410 | soft delete된 파일 |
| `THUMBNAIL_MULTIPLE_NOT_ALLOWED` | 400 | 썸네일 2개 이상 |
| `TAGS_TOO_MANY` | 400 | 태그 11개 이상 |
| `STORAGE_READ_FAILED` | 500 | 디스크/S3 읽기 실패 |
| `STORAGE_WRITE_FAILED` | 500 | 디스크/S3 쓰기 실패 |
| `STORAGE_DELETE_FAILED` | 500 | 디스크/S3 삭제 실패 |

> ❌ **본 MVP 범위 외** (v1.1): `FILE_POST_MISMATCH` (게시글 종속 파일 다운로드 검사) → 통합 도메인이라 단일 `/api/files/{fileId}` 로 모이므로 별도 검사 불필요. `DOWNLOAD_LOG_*` 관련 코드는 다운로드 로그 자체가 v1.1.

---

## 8) Message (DM)

| code | HTTP | 의미 / 발생 조건 |
|---|---|---|
| `MESSAGE_SELF_NOT_ALLOWED` | 400 | 자기 자신에게 송신 / 자기 자신과의 대화 조회 |
| `MESSAGE_CONTENT_TOO_LONG` | 400 | 2000자 초과 |
| `MESSAGE_RECEIVER_BLOCKED` | 403 | (v1.1) 차단 관계 |
| `MESSAGE_SAME_PAIR` | 400 | 어드민 conversation 에서 userA == userB |

---

## 9) Feedback

| code | HTTP | 의미 / 발생 조건 |
|---|---|---|
| `FEEDBACK_NOT_FOUND` | 404 | |
| `FEEDBACK_STATUS_UNKNOWN` | 400 | status 값 enum 아님 |
| `FEEDBACK_ALREADY_READ` | 409 | 이미 READ 인데 다시 read 요청 |
| `FEEDBACK_RATE_LIMITED` | 429 | (v1.1) 같은 IP 다회 |

---

## 10) Reserved (예약 — 아직 사용처 없음, 추가 시 본 표 갱신)

| code | HTTP | 의미 |
|---|---|---|
| `RESOURCE_CONFLICT` | 409 | 일반 충돌 |
| `OPERATION_NOT_ALLOWED` | 400 | 일반 비즈니스 제약 위반 |

---

## 11) BusinessException 사용 패턴 (Real 표준)

코드와 메시지는 enum으로 묶어 관리하는 게 좋습니다.

```java
public enum ErrorCode {
    USER_NOT_FOUND(HttpStatus.NOT_FOUND, "해당 사용자를 찾을 수 없습니다."),
    USER_EMAIL_DUPLICATED(HttpStatus.CONFLICT, "이미 가입된 이메일입니다."),
    POST_NOT_FOUND(HttpStatus.NOT_FOUND, "해당 게시글을 찾을 수 없습니다."),
    // ...
    ;
    private final HttpStatus status;
    private final String defaultMessage;
    ErrorCode(HttpStatus status, String message) { this.status = status; this.defaultMessage = message; }
    public HttpStatus status() { return status; }
    public String defaultMessage() { return defaultMessage; }
}

public class BusinessException extends RuntimeException {
    private final ErrorCode code;
    public BusinessException(ErrorCode code) { this(code, code.defaultMessage()); }
    public BusinessException(ErrorCode code, String message) {
        super(message);
        this.code = code;
    }
    public ErrorCode code() { return code; }
}

// 사용
throw new BusinessException(ErrorCode.POST_NOT_FOUND);
throw new BusinessException(ErrorCode.POST_NOT_FOUND, "게시글 #42 가 존재하지 않습니다.");
```

`GlobalExceptionHandler` 에서는 `error.code = e.code().name()`, `error.message = e.getMessage()` 로 직렬화.

> 본 Real 표준은 Mock 의 단순 `BusinessException.notFound("...")` 와 다릅니다. ErrorCode enum 도입이 Real 의 핵심 개선점.

---

## 12) 신규 코드 추가 절차

1. 본 사전에 새 코드 + HTTP + 의미 추가 (한 줄)
2. 영향 받는 명세서(03~11) 의 해당 엔드포인트 에러 표에도 추가
3. `ErrorCode` enum 에 항목 추가
4. 동일 PR 안에 코드 변경 포함
5. 다른 도메인이 같은 의미로 던질 일이 있는지 확인 (중복 코드 방지)
