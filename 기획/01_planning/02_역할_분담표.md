# 02. 역할 분담표 (8명)

> ⚠️ **2026-05-22 회의 반영** — 본 문서는 회의 결과를 반영한 패치본. 회의록 단일 진실 원본은 [`04_api/02_엔드포인트_목록_상태코드분리_수정본.md`](../04_api/02_엔드포인트_목록_상태코드분리_수정본.md).

## 회의 반영 분담 (최신 — 본 박스 기준)

| # | 파트 | 담당 인원 | 책임 요약 |
|---|---|---|---|
| 1 | **User** | 2명 (메인 + 보조, 내부 분담은 자율) | 회원가입(Form+OAuth Google/Naver), 이메일 화이트리스트, realName/major, 로그인, 권한 |
| 2 | **File (통합)** | 2명 (내부 분담은 자율) | 저장/조회/메타/삭제 통합. purpose 4종(ASSET/POST_THUMBNAIL/USER_AVATAR/REQUEST_REFERENCE). DownloadLog는 v1.1 보류 |
| 3 | **Post 영역 (Post + Request)** | 2명 (Post / Request 각각 또는 함께, 내부 분담은 자율) | 게시글 CRUD + 좋아요 + linkedRequestId 자동 완료, 요청 게시판 흐름 (어드민 개입 없음) |
| 4 | **Comment + Category + Tag/Search** | 1명 | Post/Request 댓글, 카테고리 트리, 태그, 검색 통합 입구 |
| 5 | **DM (Message)** | 1명 | REST + WebSocket + 시스템 발신자(Request 상태 알림) |
| 6 | **Infra** | 2명 (메인 + 보조, 내부 분담은 자율) | 빌드/배포/CI·CD/Docker/CORS/공통 설정/모니터링/Feedback. **DDL 게이트키퍼** |

> **내부 분담은 자율**. 페어로 묶인 2명이 본인들끼리 합의해서 영역을 나눔.
> **Admin 강한 권한 작업(분쟁 처리, 강제 삭제, 메시지 강제 열람) 은 본 MVP 범위 외.** 모든 도메인 담당자가 이 정책 공유.

> 아래 본문 옛 분담과 충돌 시 본 박스 우선.


> 비유: 한 식당의 8개 포지션. **자기 포지션의 음식만 책임지되, 주방 흐름은 같이 본다.**
>
> 단독 파트(1명)는 **가이드 + PM이 짝꿍** 역할. 페어 파트(2명)는 처음부터 같이 설계.

---

## 팀 구성 한눈에


| #   | 닉네임   | 파트                              | 페어 여부 | 책임 패키지                                             |
| --- | ----- | ------------------------------- | ----- | -------------------------------------------------- |
| 1   | (TB   | **User**                        | 페어    | `com.assetbox.user`, `common.security`             |
| 2   | (TBD) | **File-A (업로드/저장)**             | 페어    | `com.assetbox.file` 저장·업로드                         |
| 3   | (TBD) | **File-B (다운로드/로그/통계)**         | 페어    | `com.assetbox.file` 다운로드·로그·통계                     |
| 4   | (TBD) | **Post-A (게시글 본체)**             | 페어    | `com.assetbox.post` 작성/조회/좋아요                      |
| 5   | (TBD) | **Post-B (요청 게시판 Request)**     | 페어    | `com.assetbox.request` 전체                          |
| 6   | (TBD) | **Comment + Category + Search** | 단독    | `com.assetbox.comment`, `category`, `tag`(검색만)     |
| 7   | (TBD) | **DM (Message)**                | 단독    | `com.assetbox.message`, WebSocket 설정               |
| 8   | (TBD) | **Infra (2명)**                  | 단독    | `common.config`, build.gradle, CI/CD, Docker, 모니터링 |


> "Post 2명"의 분할은 회의에서 **본체(A) / 요청 게시판(B)** 로 쪼개는 게 깔끔합니다. 둘 다 게시글 형태이지만 라이프사이클이 다르기 때문에 코드 충돌이 적어요.

---

## RACI 행렬

> R(Responsible) 실행, A(Accountable) 최종 책임, C(Consulted) 상의, I(Informed) 통보


| 영역                        | User    | File-A  | File-B  | Post-A  | Post-B(Req) | Comment+Cat+Search | DM      | Infra              | PM      |
| ------------------------- | ------- | ------- | ------- | ------- | ----------- | ------------------ | ------- | ------------------ | ------- |
| 회원/인증                     | **R/A** | I       | I       | C       | C           | C                  | C       | C                  | A       |
| 파일 업로드                    | C       | **R/A** | C       | C       | C           | I                  | I       | C                  | A       |
| 파일 다운로드/로그                | C       | C       | **R/A** | C       | C           | I                  | I       | C                  | A       |
| 게시글 본체                    | C       | C       | C       | **R/A** | C           | C                  | I       | I                  | A       |
| 요청 게시판                    | C       | I       | I       | C       | **R/A**     | C                  | C       | I                  | A       |
| 댓글 (Post/Request 공용)      | I       | I       | I       | C       | C           | **R/A**            | I       | I                  | A       |
| 카테고리 트리                   | I       | I       | I       | C       | C           | **R/A**            | I       | I                  | A       |
| 태그 (검색용)                  | I       | I       | I       | C       | C           | **R/A**            | I       | I                  | A       |
| 검색 (tag/cat/keyword)      | I       | I       | I       | C       | C           | **R/A**            | I       | I                  | A       |
| DM / WebSocket            | C       | I       | I       | I       | I           | I                  | **R/A** | C                  | A       |
| 어드민 통계                    | C       | C       | C       | C       | C           | C                  | C       | **R/A**(infra가 조립) | A       |
| Docker / CI / CORS / 모니터링 | I       | I       | I       | I       | I           | I                  | I       | **R/A**            | A       |
| ERD / API 계약              | C       | C       | C       | C       | C           | C                  | C       | C                  | **R/A** |


---

## 파트별 산출물 책임 (간단 버전 — 자세한 건 `07_part_guides/` 참고)

### 1) User

- 엔드포인트: `/api/users/`**, `/api/admin/users/**`
- 엔티티: `User`, `Role` (enum)
- 공통: `JwtProvider`, `JwtAuthenticationFilter`, `CurrentUser` (이미 `common.security`에 있음, 유지보수만)
- 외부 노출 컨트랙트: `Long userId`, `String nickname`, `String avatarUrl`, `Long teamId`

### 2) File-A (저장 담당)

- 엔드포인트: 업로드는 Post-A의 multipart 안에 들어옴 → **서비스 호출만 책임**
- 엔티티: `AssetFile`
- 핵심 클래스: `FileStorageService` (인터페이스), `LocalFileStorageService` (현재 구현)
- 외부 노출 컨트랙트: `AssetFile saveAndAttach(postId, file, isThumbnail)`

### 3) File-B (다운로드/로그/통계)

- 엔드포인트: `/api/posts/{postId}/files/{fileId}` (다운로드, Post-A와 협의된 위치), `/api/admin/download-logs/`**
- 엔티티: `DownloadLog`
- 비동기: `@Async` 로 로그 적재
- 외부 노출 컨트랙트: `Resource download(postId, fileId, requester)`, `Page<DownloadStats>` 조회

### 4) Post-A (게시글 본체) + 게시판 + 댓글

- 엔드포인트: `/api/posts/**`, `/api/admin/posts/**`
- 엔티티: `Post`, `PostLike`
- 협업: File-A·File-B의 서비스를 호출(직접 엔티티 X), Comment의 카운트가 필요하면 Comment 서비스 호출

### 5) Post-B (Request 게시판)

- 엔드포인트: `/api/requests/**`, `/api/requests/{id}/comments/**`(댓글은 Comment 파트 협업)
- 엔티티: `RequestPost`, `RequestComment`, `RequestStatus`(enum)
- 상태 전이 규칙, TA 배정, 완료시 Post 연결 (Post-A 협업)
- 상태 변경 시 DM 알림 → **DM 파트의 service 호출 (REST X)**

### 6) Comment + Category + Search

- 엔드포인트: `/api/posts/{postId}/comments/`**, `/api/categories/**`, `/api/posts?...search params`
- 엔티티: `Comment`, `Category`, `Tag`
- 검색은 Post-A의 Repository에 쿼리 메서드를 추가하기보다 **자기 service에서 Spec/JPQL 작성** (오너십 분리)

### 7) DM (Message)

- 엔드포인트: `/api/messages/`**, `/api/admin/messages/**`
- WebSocket: `/ws` 엔드포인트, `WebSocketConfig` (이미 있음)
- 외부 노출 컨트랙트: `MessageService.send(fromUserId, toUserId, content)` — 다른 파트(Request)가 알림으로 호출

### 8) Infra (PM 보조)

- build.gradle, Dockerfile, docker-compose, GitHub Actions, prometheus.yml
- `common.config.*`, CORS, GlobalExceptionHandler 유지
- **DDL 게이트키퍼**: 엔티티 신규/변경 PR은 Infra 리뷰 필수
- 시드 데이터(`AdminBootstrapRunner`, `CategorySeeder`) 관리

---

## 단독 파트가 막힐 때

단독 파트(Comment+Cat+Search, DM)는 짝이 없어서 막히면 외롭습니다. 규칙:

1. **막히면 30분 이상 혼자 끙끙대지 말 것** — 슬랙 `#assetbox-help` 채널에 즉시 질문
2. PM이 매일 단독 파트 4명에게 1:1 핑(5분) — 진척 + 막힌 점 확인
3. PR이 24시간 동안 리뷰 0개면 PM이 직접 리뷰어 지정

---

## "다음에 누가 무엇을 시작하는가"가 헷갈릴 때

→ `03_의식과_체크리스트.md` 의 데일리 체크리스트 참고