# 01. 시스템 아키텍처

> ⚠️ **2026-05-22 회의 반영** — 본 문서는 회의 결과를 반영한 패치본. 회의록 단일 진실 원본은 [`04_api/02_엔드포인트_목록_상태코드분리_수정본.md`](../04_api/02_엔드포인트_목록_상태코드분리_수정본.md).

## 회의 반영 아키텍처 변경 (핵심 — 본 박스 기준이 최신)

| 영역 | 변경 |
|---|---|
| **인증** | Form + Google OAuth + Naver OAuth 병행. Spring Security OAuth2 Client 사용. 이메일 화이트리스트 필수 |
| **File 도메인** | 4종 파일(에셋·게시글 썸네일·사용자 아바타·요청 참고 이미지) 모두 한 도메인. `purpose` 로 구분. 단일 입구 `GET /api/files/{fileId}` |
| **운영 권한** | Admin은 모니터링 중심. 분쟁 처리·강제 삭제·메시지 강제 열람 같은 강한 권한은 본 MVP 제외 |
| **DM ↔ Request** | 요청 상태 변경 시 시스템 발신자가 요청자에게 DM 자동 발송. M1은 REST 폴링, M2 부터 WebSocket |
| **요청 자동 완료** | assignee가 에셋 게시글 작성 시 `linkedRequestId` 박으면 트랜잭션 내에서 요청 `status → COMPLETED` 자동 전환 |
| **브랜치 / 환경** | `feature → dev → prod(테스트) → main(배포)` 3단. prod 프로파일은 MySQL + Docker compose |

> 아래 본문 다이어그램과 충돌 시 본 박스 우선.


> 비유: **아파트 한 동(모놀리스) 안에 8개 가구(도메인)가 사는 구조.** 각 가구는 자기 현관문(서비스 API)만 열어두고, 거실 가구 배치는 마음대로. 단, 공용 시설(전기, 수도 = 공통 설정)은 관리실(Infra)만 손댄다.

---

## 한 장으로 보는 아키텍처

```
┌──────────────────────────────────────────────────────────────────────────┐
│                      클라이언트 (Vite + React, 별도 레포)                  │
│                       fetch / WebSocket(STOMP)                            │
└────────────┬──────────────────────────────────────────────┬──────────────┘
             │ HTTP(REST + JWT)                              │ WS(STOMP+JWT)
             ▼                                               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    Spring Boot 3.2.5 (assetbox)                            │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │  공통 레이어 (common)                                              │ │
│  │   - SecurityConfig (JWT 필터 + CORS)                               │ │
│  │   - GlobalExceptionHandler → ApiResponse                           │ │
│  │   - BaseEntity (createdAt/updatedAt)                                │ │
│  │   - WebSocketConfig + StompJwtChannelInterceptor                    │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│  ┌──────────┬──────────┬──────────┬──────────┬──────────┬──────────┐    │
│  │   user   │   file   │   post   │ request  │ comment  │ category │    │
│  ├──────────┼──────────┼──────────┼──────────┼──────────┼──────────┤    │
│  │ ctrl/svc │ ctrl/svc │ ctrl/svc │ ctrl/svc │ ctrl/svc │ ctrl/svc │    │
│  │ repo/dom │ repo/dom │ repo/dom │ repo/dom │ repo/dom │ repo/dom │    │
│  └──────────┴──────────┴──────────┴──────────┴──────────┴──────────┘    │
│  ┌──────────┬──────────┬───────────────────────────────────────────┐    │
│  │   tag    │ message  │   feedback                                │    │
│  │ (DM)     │          │                                           │    │
│  └──────────┴──────────┴───────────────────────────────────────────┘    │
└─────────────────┬─────────────────────────────┬────────────────────────┘
                  │ JDBC                         │ 로컬 디스크 IO
                  ▼                             ▼
        ┌──────────────────┐         ┌──────────────────────┐
        │   H2 (dev) /     │         │  ./uploads/   (dev)   │
        │   MySQL (prod)   │         │  S3 (prod, v1.1 예정) │
        └──────────────────┘         └──────────────────────┘
                  ▲
                  │ scrape
        ┌──────────────────┐
        │   Prometheus     │
        └──────────────────┘
```

---

## 왜 "모듈러 모놀리스"인가 — 비유 + 이유

- **마이크로서비스(MSA) 풀세트**는 8명이 한 달 안에 다루기엔 과체중입니다. 게이트웨이, 서비스 디스커버리, 분산 트랜잭션, K8s ... 학습 효과보다 무거움이 더 큽니다.
- 대신 **모놀리스 한 통 안에 도메인 경계를 단단히** 그어둡니다. 비유하자면 **"한 건물 안에 여러 가게가 있되, 각 가게는 자기 출입문만 두는" 쇼핑몰**.
- 이렇게 두면 나중에 한 가게(ex. file)가 커지면 그 가게만 분리해 별도 서비스로 떼어내기 쉽습니다 (= 스트랭글러 패턴 준비).

### 도메인 간 통신 규칙 (한 줄)

> **"엔티티를 빌리지 말고, 서비스를 부르거나 ID만 들고 다닌다."**

| 좋은 패턴 | 나쁜 패턴 |
|---|---|
| `RequestService` 가 `MessageService.send(...)` 호출 | `RequestService` 가 `Message` 엔티티 직접 생성·저장 |
| `Post.authorId : Long` 으로 들고 다님 | `Post.author : User` 무겁게 fetch JOIN |
| 응답 DTO에 `{ "authorId": 7, "authorNickname": "kim" }` | 응답 DTO에 `User` 통째로 |

> 단, 이미 작성된 코드(`Post.author : User`)는 JPA의 `@ManyToOne(LAZY)` 로 안전. **새로 추가하는 도메인 간 연결**부터 이 규칙을 따릅니다.

---

## 런타임 (8080 / 4040 / 3306)

| 컴포넌트 | 포트 | 비고 |
|---|---|---|
| Spring Boot 백엔드 | 8080 | `bootRun` 또는 `docker-compose up backend` |
| 프론트(Vite) | 5173 | 별도 레포 (`assetbox-front`) |
| MySQL (prod) | 3306 | docker-compose |
| H2 콘솔 (dev) | 8080/h2-console | jdbc:h2:file:./data/assetbox |
| Prometheus | 9090 | docker-compose, scrape 대상 `/actuator/prometheus` |

---

## 인증 흐름 (JWT)

```
[클라이언트]                            [Spring Boot]
   │                                       │
   ├── POST /api/users/login ─────────────▶│  비번 검증 → JwtProvider.create()
   │                                       │
   │◀───────────── 200 { token, user } ────┤
   │                                       │
   │ Authorization: Bearer <jwt>           │
   ├── GET /api/posts ────────────────────▶│  JwtAuthenticationFilter
   │                                       │  → Authentication 등록
   │                                       │  → @PreAuthorize / RoleHierarchy 검사
   │◀───────────── 200 { posts } ──────────┤
```

- 토큰 만료: 기본 7일 (`jwt.expiration` 프로퍼티)
- WebSocket 인증: STOMP `CONNECT` 헤더의 `Authorization` 을 `StompJwtChannelInterceptor` 가 검사
- 익명 허용 엔드포인트: 회원가입/로그인, 카테고리 조회, h2-console, swagger

---

## 데이터 흐름: 게시글 작성 (대표 시나리오)

```
프론트                  PostController        PostService        FileService(File-A)        FileStorage         DB
  │                          │                    │                       │                     │              │
  ├── multipart POST ──────▶ │                    │                       │                     │              │
  │                          ├── create(dto, files) ─▶                    │                     │              │
  │                          │                    ├── 1) Post 저장 ──────────────────────────────────────────▶│
  │                          │                    ├── 2) Tag findOrCreate (TagService)                       ─▶│
  │                          │                    ├── 3) files.forEach: file.saveAndAttach(postId, f) ▶      │
  │                          │                    │                       ├── 디스크 저장 ─▶                  │
  │                          │                    │                       ├── AssetFile 저장 ──────────────▶│
  │                          │                    │◀───────────────  AssetFile들 ──────────────────────────┤
  │                          │◀── PostResponse ───┤                                                          │
  │◀── ApiResponse{...} ─────┤                                                                              │
```

**핵심**: PostService가 File을 "직접 저장"하지 않고 **FileService를 부른다**. 이 한 줄 규칙이 향후 S3 전환 때 PostService를 한 줄도 안 건드리고 끝낼 수 있게 해줍니다.

---

## 비기능 단면

### 트랜잭션

- 컨트롤러 → 서비스 → 리포지토리. 트랜잭션 경계는 **서비스 메서드** 에 둠 (`@Transactional`).
- 외부 도메인 서비스를 부를 때(예: RequestService → MessageService) **같은 트랜잭션** 안에 묶입니다. 그래서 send가 실패하면 상태 변경도 롤백.
- 비동기(DownloadLog 적재)는 트랜잭션 분리. `@Async` 이후의 실패는 본 흐름에 영향 X.

### 캐시

- 인기 태그는 **Caffeine 인메모리 캐시** (`@Cacheable`, TTL 60s).
- v1.1에서 Redis 옮길 수 있도록 `CacheManager` 한 곳으로만.

### 로깅/관측

- 액세스 로그: Spring 기본 + 요청 ID(향후 MDC)
- 메트릭: `micrometer-registry-prometheus`, `/actuator/prometheus`
- 다운로드 로그: `DownloadLog` 테이블에 별도 적재 (저작권 대응)

### 보안 체크 리스트

- [x] 비번 BCrypt 해시
- [x] JWT 시그니처 검증
- [x] CORS 화이트리스트(`cors.allowed-origins`)
- [x] 어드민 엔드포인트 `hasRole('ADMIN')`
- [x] 파일 확장자/사이즈 화이트리스트
- [ ] 다운로드 URL 서명(presigned) — v1.1
- [ ] Rate limit — v1.1
